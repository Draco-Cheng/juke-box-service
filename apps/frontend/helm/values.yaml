service:
  type: ClusterIP
  port: 80
  targetPort: 80

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "100m"

# Note: VITE_API_URL is baked into the frontend at build time
# For production, set VITE_API_URL=/api in the CI/CD build step
